import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
    appId: 'jmw.dictation.app',
    appName: 'dictation',
    webDir: 'www',
    bundledWebRuntime: false,
};

export default config;
